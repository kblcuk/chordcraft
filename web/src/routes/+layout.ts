// Enable pre-rendering for all routes
export const prerender = true;

// Client-side routing (SPA behavior after initial load)
export const ssr = false;

// Disable trailing slashes
export const trailingSlash = 'never';
